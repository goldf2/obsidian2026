# web2py 服务系统架构与功能图

> 适用根目录: `/opt/<web2py_service>`  
> 运行入口: `https://<domain>`  
> 用途: 为多 app web2py 服务提供统一架构表达模板。

## 1. 系统定位

web2py 服务平台通常由一个平台 app 和多个业务 app 组成。平台 app 提供统一入口、认证、用户、角色、菜单和公共 UI；业务 app 按业务域拆分，独立维护业务数据、页面和权限。

| 层级 | 示例归属 | 职责 |
| --- | --- | --- |
| 平台层 | `<platform_app>` | 统一入口、统一认证、用户/角色/菜单管理、顶部导航、公共 UI 能力 |
| 业务层 | `<business_app>` | 某一业务域的模型、控制器、页面、报表和附件 |
| 集成层 | `<integration_app>` | 外部系统接口、定时同步、数据交换 |
| 存量层 | `<legacy_app>` | 历史功能，逐步接入统一认证、导航和数据规范 |

核心原则:

- 统一登录、用户、角色和平台菜单只维护一套。
- 业务 app 可以按业务域拆分，但不能各自复制认证和平台导航。
- 顶部导航、平台左侧菜单、app 页内菜单、页面工具栏分工明确。
- app 代码目录与运行数据目录分离。

## 2. 运行架构图

```mermaid
flowchart TB
    User[浏览器用户] --> Domain[https://<domain>]
    Domain --> Nginx[Nginx 反向代理]
    Nginx --> Web2py[<web2py-service>.service<br/>127.0.0.1:<port>]

    Web2py --> Platform[<platform_app><br/>门户 / 认证 / 系统管理]
    Web2py --> AppA[<business_app_a><br/>业务域 A]
    Web2py --> AppB[<business_app_b><br/>业务域 B]
    Web2py --> Legacy[<legacy_app><br/>存量功能]

    Platform --> AuthDB[(统一认证库)]
    AppA --> AuthDB
    AppB --> AuthDB
    Legacy -.逐步改造接入.-> AuthDB

    AppA --> AppADB[(业务库 A)]
    AppB --> AppBDB[(业务库 B)]

    Platform --> PlatformData[外置运行数据<br/>uploads / runtime]
    AppA --> AppAData[外置运行数据<br/>uploads / runtime / backups]
    AppB --> AppBData[外置运行数据<br/>uploads / runtime / backups]
```

## 3. 代码分层图

```mermaid
flowchart TB
    subgraph Platform[平台公共层 <platform_app>]
        Auth[统一认证<br/>auth_user / auth_group / auth_membership]
        Nav[统一导航接口<br/>顶部 / 侧栏 / 页内]
        UI[公共 UI 能力]
        Admin[系统管理控制器]
    end

    subgraph BusinessA[业务 app <business_app_a>]
        MenuA[app 菜单]
        ModelsA[业务模型]
        ControllersA[业务控制器]
        ServicesA[业务服务层]
        DomainA[领域规则层]
    end

    subgraph BusinessB[业务 app <business_app_b>]
        MenuB[app 菜单]
        ModelsB[业务模型]
        ControllersB[业务控制器]
        ServicesB[业务服务层]
        DomainB[领域规则层]
    end

    Nav --> MenuA
    Nav --> MenuB
    UI --> BusinessA
    UI --> BusinessB
    Auth --> BusinessA
    Auth --> BusinessB
```

## 4. 平台能力图

```mermaid
mindmap
  root((web2py 服务平台))
    平台基础
      统一登录
      用户管理
      角色组管理
      菜单管理
      系统配置
      公共 UI
      运行诊断
    业务域 A
      看板
      主数据
      业务单据
      附件管理
      报表
    业务域 B
      采集
      记录
      审核
      异常闭环
      统计分析
    存量系统
      历史入口
      iframe 兼容
      逐步迁移
```

## 5. 导航架构图

```mermaid
flowchart LR
    Top[顶部导航<br/>response.menu] --> Home[平台首页]
    Top --> TopA[业务域 A]
    Top --> TopB[业务域 B]
    Top --> TopSystem[系统后台管理]
    Top --> TopLegacy[存量功能]

    Sidebar[平台左侧菜单<br/>response.sub_menu] --> SideHome[平台首页]
    Sidebar --> SideA[业务域 A]
    Sidebar --> SideB[业务域 B]
    Sidebar --> SideSystem[系统后台管理]
    Sidebar --> SideLegacy[存量功能]

    PageMenu[app 页内菜单<br/>response.page_menu] --> AppPage[当前 app 内部页面]
    Toolbar[页面工具栏] --> Actions[新增 / 编辑 / 导入 / 导出 / 审核 / 刷新]
```

导航边界:

| 区域 | 归属 | 说明 |
| --- | --- | --- |
| 顶部导航 | 平台级入口 | 面向系统级或业务域级入口 |
| 平台左侧菜单 | 平台和业务域入口 | 最多两级，不放页面动作 |
| app 页内菜单 | 当前 app 内部页面入口或功能组切换 | 由业务 app 自己维护 |
| 页面工具栏 | 当前页面动作 | 新增、编辑、导入、导出、审批等动作 |

导航变量契约:

| 变量 | 架构归属 | 职责 |
| --- | --- | --- |
| `response.menu` | 顶部导航 | 平台级入口，由平台 app 统一生成，各 app 统一渲染 |
| `response.sub_menu` | 平台左侧菜单 | 平台和业务域入口，由平台 app 统一生成，各 app 不得挪作内部菜单 |
| `response.page_menu` | app 页内菜单 | 当前 app 内部页面入口，由业务 app 自己维护 |

这些变量是跨 app 的系统级契约。开发规范和项目实现只能遵守这个契约，不应重新定义变量含义。

## 6. 数据关系图模板

```mermaid
erDiagram
    AUTH_USER ||--o{ AUTH_MEMBERSHIP : has
    AUTH_GROUP ||--o{ AUTH_MEMBERSHIP : contains

    BUSINESS_ENTITY ||--o{ BUSINESS_RECORD : owns
    BUSINESS_RECORD ||--o{ BUSINESS_ATTACHMENT : has
    BUSINESS_RECORD ||--o{ BUSINESS_AUDIT_LOG : produces
```

建议:

- 认证表由平台 app 统一维护。
- 业务表归属于业务 app 的独立数据库或清晰命名空间。
- 附件实体应放在 app 外部运行数据目录，数据库只保存文件名、业务归属和元数据；具体路径以数据代码分离规范为准。

## 7. 应用矩阵模板

| App | 类型 | 认证方式 | 导航方式 | 业务库 | 运行数据目录 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| `<platform_app>` | 平台 app | 自身维护 | 顶部/侧栏来源 | 认证库 | 外置运行数据目录 | 当前 |
| `<business_app_a>` | 业务 app | 共享认证 | 平台导航 + app 页内菜单 | 独立业务库 | 外置运行数据目录 | 当前 |
| `<business_app_b>` | 业务 app | 共享认证 | 平台导航 + app 页内菜单 | 独立业务库 | 外置运行数据目录 | 当前 |
| `<legacy_app>` | 存量 app | 待统一 | 存量入口 | 待梳理 | 待梳理 | 待改造 |

## 8. 待确认清单

- 哪个 app 是平台 app。
- 哪些业务域需要拆成独立 app。
- 哪些存量 app 保留 iframe 兼容入口。
- 哪些 app 需要独立业务库。
- 哪些运行数据目录还在 app 代码目录内，需要迁移到外置运行数据目录。
